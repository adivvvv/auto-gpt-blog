RewriteEngine On
RewriteBase /

# if a real file/folder, serve it
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# otherwise, route to front controller
RewriteRule ^ index.php [L]